# A1：本文保存と行動訂正の分離

2026-09-14。A1初回候補は不採用。実装を基準版へ戻し、この記録と差分をarchivedへ保存した。

## 実装した範囲

- 有効なdraftを先に検証し、次行動が不正でも本文・Knowledge・引用・回復状態を同じDBトランザクションで保存する。
- 行動の訂正には専用の `deliverable_navigation_repair` を使用する。現在稿は送らず、節見出し・未解決点・限界・失敗した操作・具体的な修正方法・候補を送る。返却draftはnullに制限する。
- 行動訂正は局所2呼び出し、ジョブ全体6呼び出しまで。上限では `partial / navigation_recovery_limit` とし、本文を保持する。通常の執筆指示は維持した。
- 新規ジョブに `deliveryRecoveryVersion=1` と上限を保存する。設定のない旧ジョブは旧回復方式を維持する。
- 既存のsequenceとoperation ledger、store.commitの原子性によって、保存失敗後も同じLLM結果を再利用する。適用したoperation IDも保存状態へ記録する。
- Compatible APIの `finish_reason=length` を保持し、JSONとして解析できても途中切断として拒否する。使用量は通常どおり精算する。Codex SDKのTurn型には終了理由が公開されていないため、同等の検知は保証しない。JSONの推測補完は行わない。

候補ID化（A2）、通常SystemContextの整理（B）、節単位の更新（C）は含めない。原因の切り分けのため、A1を採用できるか確認してから進める。

## 検証

全体の `bun run verify` は38ファイル252テストを含めて成功。その後、DB保存中の失敗とCompatible APIの途中切断の回帰ケースを追加し、専用10テストを再実行して成功した。新規ファイルのlint警告はない。

回帰ケースは、有効本文＋不正行動、同じ既読URLの反復後の回復、局所上限、全体上限、不正引用、行動schema違反、途中切断、旧版互換、保存失敗後のoperation再利用。各tickでEngineを作り直し、メモリ上の状態に依存しないことを確認した。保存失敗のケースは書き込み後にLeaseLostを発生させてトランザクションをロールバックし、本文とLLM呼び出しが重複しないことを確認している。

通常live試行: `ae0add87-1731-4059-a730-54ec1a146c72`。テーマは `deepseek flash 4.1`、luna/low、基準と同じ予算。記録は `data/evaluations/topic-rotation-20260913/07-deepseek41-luna-a1/`。開始時点のソースhash・差分、LLM監査、使用量、本文各版を保存している。dataはgitignore対象。

この試行はA1の初期確認であり、計画の4テーマ複数回の採用比較を完了したことにはならない。機械的な失敗回復の成功と、探索の品質向上は別々に判定する。


# A1通常探索の評価: 65 / 100

2026-09-14。ジョブ ae0add87-1731-4059-a730-54ec1a146c72、luna/low、終了 partial / unresolved_questions。

|固定項目|点|根拠|
|---|---:|---|
|対象同定・回答|12/15|モデル概要・料金・API提供を説明したが、技術の中心と利用条件が未解決。GET /modelsの実応答まで中心の問いに追加し範囲が広がった。|
|探索・情報増分|13/25|公式料金や独立資料へ到達し不正行動から回復。ただしAPI一覧・第三者提供告知への寄り道と重複が多い。費用対情報増分は2/5を含む。|
|根拠・条件|15/20|公式料金、第三者ゲートウェイの観測値、ライセンスの帰属を区別。一次技術本文は未読で、転載値の条件や用語の精度を確定できていない。|
|説明|12/20|具体値は増えたが8B/16Bや提供状況を資料ごとに繰り返す。基準06のCSA2、層間再利用、学習・評価条件より浅く、API周辺の説明が中心。|
|Knowledge|7/12|7件に分散。適用・非適用条件はあるが、実値を失った一般的注意とモデル一覧説明などが並び、基準のまとまりに及ばない。|
|Episode|6/8|未読、bot challenge、不正URL、未解決終了を記録。ただし調査内容の再要約が多く、訂正でどう回復したかは具体性が弱い。|

A1の実装上の効果は確認できた。event3269で未発見URL https://api.deepseek.com/models を拒否し、本文は保持。delivery:34:deliverable_navigation_repairはdraft:nullで別の既知候補を選び、1回で回復した。実測は入力10,858・出力277で、全体使用量の内数。拒否URLの外部取得は実行していない。今回の回復開始時は既存本文維持の応答であり、「新しい有効本文＋不正行動」を同時保存するケースの実証は回帰テストによる。

通常探索全体は入力279,511・出力44,358・合計323,869 tokens、964.273秒。検索4、URL試行15、保存資料10。取得ガード5操作と検索poll1操作は使用量不明で別記。予約値を実測へ加えず、cached/reasoningも二重加算しない。新規7試行累積は入力1,460,366・出力173,604 tokens。

基準06は70点、入力221,553・出力22,386。今回の合計消費は約32.8%多い。機械的回復が機能しても、レポート品質の改善とは言えない。取得条件・探索経路・生成の変動があり、A1が悪化の原因であるとは断定しない。4テーマ複数回の採用条件には未到達。ユーザーの保守的な差し戻し方針に従い初回候補を採用せず、mainの実装は直前の採用版へ戻す。A2/B/Cをこの候補に積み重ねない。

今回の判断はA1初回実装候補の不採用であり、Piの設計原則全体を反証したものではない。候補差分・回帰テストはarchivedに保存し、再設計時の比較資料にする。

復元用差分: [A1-CANDIDATE.patch](A1-CANDIDATE.patch)。適用先は `8111576`。元の実装ファイルとテストを含むが、採用済みコードではない。
