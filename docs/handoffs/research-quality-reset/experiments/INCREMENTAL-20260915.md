# 小変更による段階改善（2026-09-15）

## 固定条件

基準main e363eb5。探索はgpt-5.6-luna / low。共通ガイドの配点15/25/20/20/12/8、予算、4テーマの同テーマ対比較を使用。80点未満でも前段階より改善した変更はユーザー指定に従い段階採用候補とする。80点達成は複数テーマで確認する。

## コード・生ログからの初期観測

P0基準の生ログ e2e13d3f-eeca-4a70-b114-ab2f75121297 を確認。event37で2資料目を読み、event40で WITHHELD_SOURCE_ALIAS_USE_INDEPENDENT_EVIDENCE、最終レポートは概要ページの機能一覧にとどまる。本文の引用は概要ページの範囲を支持するが、仕組みや具体例が不足。Knowledgeは機能領域を横断する一般則に偏る。

コードでは入力候補をattemptedとdepthで除外する一方、行動検証ではsameSourceによる拒否資料の別名も除外する。提示した候補と実行可能候補が一致しない可能性がある。次の仮説は、この既存の禁止条件を入力候補にも適用すると、不正URLの選択・書き直しが減るというもの。ガード判定そのものや回復上限は変えない。資料多様性や情報増分の減少、誤除外が反証条件。

この観測は過去レビューを読む前に行ったが、初版ファイル保存前にLUNA-P0.md内の過去評価も表示してしまった。厳密な盲検の独立評価とは扱わない。過去点数を新規基準の点数に流用しない。

新規基準: PostgreSQL 19 / 0723b926-249a-4651-8f71-7d446be91e22。実行中はソース変更を行わない。

## 変更候補 S1

範囲: apps/worker/deliverable-engine.tsの候補入力、必要なら共通の候補可否関数とその回帰テスト。候補ID方式全体への移行より小さく、既存検証との整合だけを試す。引用・本文保存・Episode・採点・モデルは今回の変更対象外。

関連テストとbun run verify後、候補コミットとcodex/experiment-incremental-s1-20260915を保存・push。悪化時は候補をrevertし記録を残す。同じテーマの基準→候補で初期評価、良ければテーマを交代して逆順も比較する。外部条件の違いは別記する。

## S1仮説の訂正（実装前）

監査入力の照合で、過去のdelivery9/10が再選択したURLはsources.linksにもdiscoveriesにも存在しなかった。候補除外案は観測した停止原因を説明しないため、実装前に取り下げる。DeepSeek基準06の訂正でもURL_ALREADY_ATTEMPTED後に同じ公式URLを再選択している。

S1を「訂正フィードバックへ拒否したnext操作を付ける」に限定する。現状のvalidationErrorはエラー名のみ。失敗したURL/sourceId/queryと実行されなかった事実を明示し、現在の候補で選び直すよう伝える。既存の1回訂正、本文の再送、保存順序は変えない。A1の構造変更や新しいLLM処理は導入しない。

反証条件: 訂正で同じ不正操作を再選択する、無関係な成果物修正を増やす、品質/正常完了を下げる、token増分に見合う改善がない。作用するのは検証エラー後だけで、エラーのない実行の品質差は本変更の効果と解釈しない。

## 次候補として記録する問題（未実装）

新規基準のversion5では、短いテーマ名から「最終版の確定仕様」「実運用で性能を再現できるか」をopenQuestionsの必須条件に追加している。版の不確実性を明示するのは正しいが、Web調査で実施できない実機試験や未来の確定を満足条件にすると、説明を深める探索より未充足の維持へ偏る可能性がある。S2候補は、公開済みの版・時点に範囲を合わせ、実機試験・将来確定を必要なら限界として扱う短い契約修正。S1との混合はしない。

## PostgreSQL 19 新規基準: 69/100

0723b926-249a-4651-8f71-7d446be91e22。partial / unresolved_questions。9分0.7秒。入力178,783 / 出力25,081（この改善ループ累積も同値）。検索4、取得9、保存7。訂正エラー0。検索4回目でbot challenge、2件ガード未読。

|項目|点|根拠|
|---|---:|---|
|対象・回答|12/15|最終v8冒頭でBeta3/開発資料/RDS Beta1を区別。主要機能の説明はあるが、最終版確定や実機性能再現を必須課題に追加。|
|探索・増分|16/25|event3447で本文PDFへ進むが拒否。3486のJPUG PDF、3504の公式テンポラル仕様、3530の独立RDS実測へ到達。プレスリリース・目次の重複、最終版探索の反復があり、費用対増分3/5。|
|根拠・条件|15/20|引用は読んだ本文を支持し、RDSとコミュニティ版を区別。概要2段落目は動的有効化を「確認した」と広く述べ、後半の未検証説明と緊張。長い引用範囲が残る。|
|説明|13/20|期間制約・グラフ検索・REPACKの具体例あり。ただしv5のIVM制限、部分更新測定、WAL圧縮やプラン助言の仕組みがv7以降で一般的な列挙へ縮小。全体が機能カタログ寄り。|
|Knowledge|8/12|4件。期間制約、SQL/PGQ、再編成は単独で理解可能。具体SQL・数値は薄く、Beta利用条件が一部本文から外れ、一般注意則も残る。|
|Episode|5/8|1件。bot challengeと未読・未解決は正しい。独自に広げた正式仕様確定を原依頼の意図にし、RDS記事での未検証を自分の失敗と同列に置く。判断の改善に使える経路説明が薄い。|

最終v8はv7と同文。保存した行動台帳はdata/evaluations/incremental-20260915/pg-baseline/ledger.md。監査入力・出力はrun-detail.json内operations。操作のtokenUsage未記録はusage.jsonへ分離し、予約を実測へ加算していない。source本文hashも保存。基準全体検証は37ファイル242テスト、typecheck/lint/format/build/docs成功（既存lint警告あり）。

## S1初期対は比較不能・採用保留

候補ce1c3f2、保存ブランチcodex/experiment-incremental-s1-20260915（リモート確認済み）。全体verify成功。候補PostgreSQL 19: 633d985c-cf8f-43a0-bac9-a8a1866dcdc3。最初の検索がタイムアウトし資料0件で終了。訂正エラー0、変更分岐未到達。点数の勝敗から除外し、S1の回復効果は未検証。4b02429でmainから候補のみrevertしpush済み。

候補実測入力10,901 / 出力534。2試行累積入力189,684 / 出力25,615。基準では7資料取得できたため外部条件が比較不能。正常完了0/2だが有効な比較対は0。基準WebUIでレポートv8、Knowledge4、Episode1、履歴と使用量を確認。候補の本文なし終了もAPIへ保存。

次はテーマをDeepSeek flash 4.1へ変更し、新規基準を取得する。S1へ追加変更は積み重ねない。

## DeepSeek 新規基準: 59/100

bfd61811-4899-44a0-a0b4-1bf17e3dc3dd、基準4b02429。partial / invalid_deliverable。入力126,981 / 出力14,386、約359秒。3試行累積入力316,665 / 出力40,001。

|項目|点|根拠|
|---|---:|---|
|対象・回答|12/15|最終v4で正式名称、MoE、入出力計算量、API指定を説明。詳細機構・条件は未回答。|
|探索・増分|10/25|公式→モデルカード→技術レポート→独立解説。3取得拒否・追加検索タイムアウト。event3621で独立解説を読みながらdelivery17/18で同じ取得済み告知URLを連続選択し更新不能。費用対増分1/5。|
|根拠・条件|16/20|公式の主張と独立測定を混同せず、2,000GPUを最小要件にしない。最終引用12件はUTF16原文一致。未読モデルカードを明記。再現条件は不明。|
|説明|9/20|1255文字。構成数値・提供情報が中心でWHY/HOWが薄い。独立解説の内容は有効成果物へ保存できなかった。|
|Knowledge|6/12|API指定とキャッシュ評価の2件。使える条件付き説明だが短く、仕組みや具体例の厚みは不足。|
|Episode|6/8|失敗・未読・検索タイムアウト・Xetポインタと本文の区別を記録。訂正で同じURLを再選択した過程は曖昧。無効終了でも保存済み概要はある点の説明が弱い。|

S1が対象とする失敗が新規基準で再現した。delivery17/18はいずれもhttps://api-docs.deepseek.com/news/news260910を選択。候補一覧にない取得済みURLをエラー名だけの訂正入力でも再選択した。S1同一コードを再適用し、このテーマで初期対を再試験する。

## 固定入力の訂正比較（liveとは別）

DeepSeek基準delivery18の実入力をそのまま使い、validationErrorのS1追記だけを変更してluna/lowへ各1回。基準は同じ取得済みAPI告知URLを再選択。S1は入力候補に存在する未取得の公式別URLへ変更。両草稿ともmaterialize成功。ただしS1のURLも既読発表の別名であり、情報増分は疑わしい。局所的な実行可能性改善であって探索品質合格ではない。生成のみでWeb取得はしていない。

再生の実測: 基準入力21,696 / 出力2,390、S1入力21,781 / 出力2,689。再生計入力43,477 / 出力5,079。3件の完了live試行と合わせた累積入力360,142 / 出力45,080（進行中live候補は完了時に加算）。監査と再現スクリプトはdata/evaluations/incremental-20260915/correction-replay-*。

S1再適用コミット832aaceはce1c3f2とapps/packages/tests差分ゼロ。codex/experiment-incremental-s1-deepseek-20260915へpush、verify再通過。live候補36d1a0f2-7cc3-48c8-ab71-173e901f89c9を実行。

## S1 DeepSeek live結果: 64/100、初期候補は不採用

候補36d1a0f2-7cc3-48c8-ab71-173e901f89c9、partial / invalid_deliverable。入力194,011 / 出力17,291、約449秒。基準59点から5点増だが、S1が発動したevent3744より前にversion5は保存済みで、増分は追加資料を読めた差に由来する。終了と本文を守る効果は確認できなかった。引用23件は原文一致。

|項目|点|根拠|
|---|---:|---|
|対象・回答|12/15|版・API・構成を説明するが、料金や中心の実装詳細が未解決。|
|探索・増分|13/25|5資料、独立解説2件でCED/CSA2/FP4/評価条件の説明増。提供サービスの取得失敗も反復。訂正は別の取得済みURLを選び失敗。費用対増分2/5。|
|根拠・条件|14/20|二次資料数値の帰属と限界は明記。一方、公式本文にある転送開始時刻を未確認と扱い、技術レポート未読のまま詳細値比較に依存。|
|説明|13/20|1842文字。20層構成、KV量、複数ベンチと条件が増えるが機構は短く、数値列挙が多い。|
|Knowledge|8/12|5件。キャッシュ機構と用途を関連付け、評価条件を整理。ただし短いAPI指定と一般注意も混在。|
|Episode|4/8|未読・無効終了を記録するが「拒否後に別資料確認へ切り替えた」と述べ、実際には訂正後も取得済みURLで停止した事実を誤る。|

基準delivery17/18は同じAPI告知URL。候補delivery28は同じAPI告知URLを再選択、S1フィードバック後delivery29では既に読んだ別の公式発表URLを選び直し、やはりURL_ALREADY_ATTEMPTEDで終了。新しいURLへの実取得は行われていない。

候補は3検索、基準は2検索目でタイムアウト。共通の公式2本文、Xetポインタ、独立解説に加え候補だけ別の独立ガイドを取得。外部取得条件と経路差があるため点差の因果主張はしない。局所再生では有効URL選択が見えたが、liveでは回復せず、正常完了は両者0。初期候補を採用せずae2e301でrevertしpush。保存ブランチは保持。

4件live累積入力510,676 / 出力57,292。固定入力再生2件も含めた今回全体は入力554,153 / 出力62,371。予約は除外。不明使用量を0として足さない。

## S2: 終了前の具体例（仮説、実装前）

可逆圧縮の新規基準a0e64c10-9046-46a9-9bf0-50c0e0bdcfd0はe-Wordsの用語説明だけでsatisfiedを選択（event3777）。最終草稿は定義・用途・方式と形式の列挙で、ランレングス/ハフマン/LZ等の入力→処理→復元を説明していない。既存プロンプトのWHAT/HOW/WHY要件はあるが、LLMの終了判断に反映されていない。

S2の実装はdeliverable_step指示1か所へ、方式名等の列挙は仕組みの説明ではなく、広い技術概説の終了前に出典付きの入力→出力の具体例と条件・トレードオフを得る、得られなければ説明不足を残すという2文を追加する。テーマ固有の方法名・URLは入れない。schema、終了ガード、予算、モデル、保存方式は変更しない。

予測: 用語説明後に方式の具体例を読む行動が増え、仕組み・例・比較の配点が改善する。反証: 形式だけの例や捏造、無関係な資料への寄り道、費用増だけで説明が改善しない、過剰な未充足。固定配点は変更しない。以前記録した将来版・実機試験へのスコープ拡大対策は未実装で、本S2へ混ぜない。S1コードは差し戻し済み。

## 可逆圧縮 新規基準: 52/100

a0e64c10-9046-46a9-9bf0-50c0e0bdcfd0、completed / satisfied。入力31,720 / 出力2,208、約76秒。本文479文字、1資料、引用7件原文一致、Knowledge1・Episode1。

|項目|点|根拠|
|---|---:|---|
|対象・回答|11/15|定義、可逆/非可逆の境界、用途を回答。圧縮・復元の仕組みを説明しない。|
|探索・増分|8/25|Wikipedia未読後e-Words1件だけで終了（event3777）。方式名を認識しても具体例へ検索・リンク追跡せず。費用対増分3/5。|
|根拠・条件|16/20|用語辞典本文に沿う定義と用途。形式全体と内部の符号化を区別。独立資料・性能条件はない。|
|説明|6/20|5段落。方法・形式のカタログでHOW/WHYと入出力の例が欠ける。|
|Knowledge|5/12|完全保持が必要なら可逆という1則。利用判断には使えるが仕組み・具体例の説明が薄い。|
|Episode|6/8|未読・読了・範囲と詳細未確認を区別。成功判断は実行記録に一致するが、経路の教訓は一般的。|

通常完了を品質合格と同一視しない。S2はこの終了判断を対象にする。研究内の採点LLMや形式チェックの追加は行わない。5件liveと訂正再生の今回累積入力585,873 / 出力64,579。

## S2 可逆圧縮: 64/100、改善行動と回帰を併記

候補6045311、保存ブランチcodex/experiment-incremental-s2-20260915。実行b211d983-2e34-4d34-a890-37f73d0a007d、partial / invalid_deliverable。入力334,691 / 出力30,478、検索5・取得16・保存7。基準52点から12点増だが、正常完了は1→0、入力10.55倍・出力13.80倍。最終1484文字、Knowledge3・Episode1。全23引用は原文範囲一致するが、意味の支持は別問題。

|項目|点|根拠|
|---|---:|---|
|対象・回答|11/15|定義とDEFLATE機構、形式比較を説明。一方「完全な展開器の実装」を短い原依頼の中心課題へ追加した。|
|探索・増分|14/25|共通e-Words本文hash一致。用語読了後にworked exampleを取得する予測行動が発生。Go実装、zlib、図解、独立比較と論文へ進んだ。ただし全実装の追跡で取得拒否を重ね、event3949で拒否資料別名を選び停止。費用対増分1/5。|
|根拠・条件|13/20|比較環境と窓サイズを明記。しかし最終本文はLZMAの2.5%改善・3.5倍展開時間の比較相手をDEFLATEと誤記。読了PDFの表3ではLZMA:9=3.696/71.8、Brotli:11=3.607/257.4、DEFLATE:9=2.742/217.4であり、比較相手はBrotli。引用文字一致だけでは発見できない。|
|説明|13/20|重複コピーと動的表の機構を説明。ただしv5→v6で1957→1028文字に縮小、v7の現行ツール比較と具体入力例がv8で消失。数値比較が本文の多くを占める。|
|Knowledge|8/12|3件は条件付きの方式選択・重複コピー・展開概略。aaaaa例は初期リテラルaaが省かれ、単独利用には補足が必要。完全手順として偽装はしていない。|
|Episode|5/8|未読と失敗を区別するが、勝手に拡張した完全実装を目的とする。invalid終了を成果物そのものの不成立と同一視し、保存済みの有用本文との区別が弱い。|

引用の比較対象誤り、内容脱落、正常完了と費用の回帰から、そのままの採用は不可。画像テーマはこの誤りを確定する直前に開始済みのため完了まで記録し、その後候補を差し戻す。画像は候補先行の探索的な追加試行であり、規定の基準→候補の採用対に数えない。

完了6 live＋固定入力再生2件の累積は入力920,564 / 出力95,057。S2の4テーマ各2対は未実施、80点達成ではない。

## 次候補 S3: 草稿再生成による説明の脱落を減らす（実装前）

PG基準v5→v7、可逆圧縮S2 v5→v6とv7→v8で、新しい出典を読むほど既存の具体例・比較条件が消えた。既存指示の「Preserve existing well-supported content」は具体的な更新方法を指定していない。S3はこの1文を「既存の支持された段落と引用をそのまま残し、新しい根拠は追加または明確に該当する段落へ統合、修正・矛盾・重複がある段落だけ置換」と具体化する小さなプロンプト差分を候補とする。S2の具体例探索強制は除去してから実装する。

期待する変化は、新規資料を読んだ後でも以前の入力例、説明機構、実測条件が最終版に残ること。反証は説明の重複・長さだけの増加、誤った段落の固着、引用の不整合、費用の増加に見合う品質改善がないこと。まず固定入力で該当する更新を比較し、liveではテーマを再交代する。構造的な節ID/差分更新は別案であり混ぜない。

### S3 固定入力の予備診断（liveではない）

PG基準delivery:25:deliverable_stepの実監査system/content/schemaを使い、systemの保持指示1文だけを置換。基準とS3をluna/lowで各1回生成、source取得なし、mainの実行中S2コードは変更しない。基準15既存段落中2段落、S3は10段落が完全一致で残った。双方の草稿はmaterialize成功。単純な一致数だけを品質点とせず、基準が落とした期間範囲の具体例、PRIMARY KEYとFOREIGN KEYのSQL、WAL圧縮方式がS3では残り、新規RDS検証の環境・PGQ・REPACK説明も追加できたことを確認した。

ただし新たな基準生成も3211文字と過去の実出力1992文字より詳しく、生成の揺れが大きい。S3は3515文字。双方が同じ公式目次URLを次行動に選んでおり、探索継続の適法性・完了品質は証明していない。採用判断はliveで行う。

実測は基準入力20,327/出力5,431、S3入力20,365/出力5,623。差は入力+38・出力+192。診断計入力40,692/出力11,054。完了6liveと診断4件の累積入力961,256/出力106,111。スクリプトと入力・結果はpreservation-replay-*へ保存。これは候補実装前のリクエスト差分診断であり、S3実装・保存・verifyは未実施。

## S2 OpenAI image2.5: 67/100（候補先行の追加試行）

3ca4bb21-52cf-49b5-9ce4-10a4c2eceedf、partial / unresolved_questions、約659秒。入力186,717/出力30,750、検索2・取得10・保存8。29引用は原文一致。検索2回目のタイムアウト後、event4016で取得済みURLを選んだが、既存の訂正だけで日本語公式発表へ復帰した（S1なし）。最終1733文字、Knowledge5/Episode1。

|項目|点|根拠|
|---|---:|---|
|対象・回答|12/15|ChatGPT機能、API両モデル、提供日と料金を説明。実装仕様・編集例は未解決。|
|探索・増分|16/25|公式→独立実測→両モデル→System Cardで条件を深めた。公式の言語別再取得と発表再掲もあり、費用対増分2/5。|
|根拠・条件|14/20|少数試行・後処理クロップ・敵対的評価と通常利用の区別は正しい。本文の「安全な表示の差」は引用のunsafe-shownと逆で、limitationsでは正しくUnsafe Generation Presentedと書く不整合。長いモデルページ引用も残る。|
|説明|12/20|Sketchの役割分担、部分編集、安全スタックを説明。しかしv7の2538文字→v8の1733文字へ縮小し、品質設定・モデルID・操作の詳細が消える。最後に読んだ安全性説明へ重心が移る。|
|Knowledge|7/12|用途選択、部分編集、Sketch、評価解釈の5件。単独利用可能だが短い抽象則が多く、具体プロンプト・操作例が薄い。|
|Episode|6/8|タイムアウト、取得拒否、無効URL、未解決の説明は実記録と整合。訂正後の具体的な回復経路や資料重複の教訓は弱い。|

S2は318d64eで差し戻し・push済み。remote保存6045311を確認後に実施。画像の同日基準5f7ac40b-fed0-4647-9098-a997730c5e8eを開始した。S2不採用は可逆圧縮の重大な回帰によるもので、画像の点数単独で因果を主張しない。

完了7live＋診断4件累積: 入力1,147,973/出力136,861。今回の全verifyはS2まで成功、S3はまだ未実装。

## OpenAI image2.5 新規基準: 58/100

5f7ac40b-fed0-4647-9098-a997730c5e8e、318d64e、completed/satisfied。入力25,478/出力2,339。公式発表1件だけで終了（event4087）、本文802文字、Knowledge2/Episode1。7引用は原文一致。S2と共通の公式発表本文hashは一致。基準に外部失敗なし、S2には追加検索タイムアウト・取得拒否と追加7資料。

配点: 対象12（製品・2モデル・用途の回答）、探索8（公式発表後に詳細・独立比較へ進まず、費用対増分3/5）、根拠16（公式主張と独立測定を区別、狭い証拠範囲）、説明10（機能と反復編集の狙いは説明するが具体例・実装条件は薄い）、Knowledge6（2件、用途と変更範囲指定の条件付き説明）、Episode6（実経路と限界を記録、浅い探索を見直す具体判断は弱い）。S2は+9点だが入力7.33倍/出力13.15倍、正常完了1→0、比較閾値の誤訳と草稿脱落があり採用しない。

S2の完了2試行はいずれも品質80未満。初期基準2件は両方正常完了、候補2件は両方partial。少数試行の観測であって固定の成功率ではない。全体の完了8live＋診断4件累積入力1,173,451/出力139,200。WebUIで両画像試行と可逆圧縮S2のレポート・メモリー・履歴・使用量を確認。PG資料なし候補も限界の説明と使用量を画面確認した。
