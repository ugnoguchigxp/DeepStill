# 段階改善の現在地（2026-09-15）

> この文書は2026-09-15終了時点のスナップショット。以後のL1・SystemContext S1と現在の採否は [../TRIAL-HISTORY.md](../TRIAL-HISTORY.md) を参照する。

80点は未達。探索モデルはgpt-5.6-luna / low固定。採用済みソースは変更なし。main上の39aceaaはS4Cの評価中候補で、採用宣言ではない。

|変更|同テーマの基準→候補|判定|
|---|---|---|
|S1: 失敗操作を訂正入力へ添付|PGは検索失敗で比較不能、DeepSeek59→64|増分は変更発動前の追加資料による。live訂正に失敗し不採用。|
|S2: 終了前に具体例を求める|可逆圧縮52→64、画像58→67|深掘りは増えたが比較対象誤り、内容脱落、費用と正常完了の回帰。不採用。|
|S3: 既存段落の保持を具体化|PG69→59|保持は改善したが引用ID欠落で停止。不採用。|
|S4C: 引用元IDの候補を制約|DeepSeek60→62、画像58→69、可逆圧縮は検索障害|実験基準として保持。最終採用前。|

基準点はその試行の点であり、コードの固定性能ではない。固定配点は15/25/20/20/12/8。正常完了も別に評価する。最初の試行で悪化した候補を差し戻し、remote保存ブランチと証跡を残している。4テーマ各2対の採用検証は未達で、単発の点数で80点達成としない。

[詳細な採点・実行ID・反証・費用](INCREMENTAL-20260915.md)。生ログはdata/evaluations/incremental-20260915/。完了13live＋診断4件の実測累積は入力1,960,146/出力232,313（進行中候補の予約を除く）。


## PG再試行の中断と再開条件

S4CのPG試行 `6fb33211-17c5-49ce-a4de-1b905e959492` は、最後の資料末尾を読んだ後の `delivery:30:deliverable_step` が180秒でabortedとなった。4資料・3検索・12URL、最後の保存稿はv4/1,830字。未完了のため採用比較には使わない。

確認済み使用量は入力162,062/出力11,583。未確定要求の予約61,674/12,000は実測に加えない。14live（中断1件含む）と診断4件の確認済み累積は入力2,122,208/出力243,896、これに外部LLM要求1件の使用量不明がある。ジョブ集計の予約込み値を使用量として合算しないようローカル集計を修正した。

通常resume APIは409 `EXTERNAL_STOP_CONFIRMATION_REQUIRED`。workerは生存しているが子プロセスは見当たらず、該当SDKセッションにはtask_startedのみでtask_complete/usageの完了記録がない。これは外部推論停止の証明ではない。`spec/round-research-plan.md:225`に従い、外部完了・取消の確認またはオペレーターによる外部処理終了の明示確認を待つ。DB、予約、ブロックを手動解除していない。再開前証跡は `data/evaluations/incremental-20260915/pg-s4c/failed-before-resume/` に保存済み。

次の候補はS4C上でS3の段落保持指示を一つだけ再試験する案。まだ実装していない。S4Cは実験途中の保存候補で、最終採用でも80点達成でもない。画像S4CのWebUI本文・使用量は確認済み、同試行のmemory/history画面と圧縮S4Cのfallback画面の追加確認は未了。
