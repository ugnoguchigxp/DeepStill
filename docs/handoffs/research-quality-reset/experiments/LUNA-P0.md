# Luna P0：配信範囲と草稿保存範囲の分離

2026-09-14。提案元：ユーザーが共有したlunaの案（提案時reasoningは不明）。探索はluna/low。基準コードは `43d283b`。A1パッチは適用していない。

## 事実と仮説

基準実装はreadでcursorsを進める。ただし検証失敗時もcontentを保持し、訂正時に再送している。「検証失敗だけで必ず次の範囲へ飛ぶ」という説明はコードからは支持されない。問題は配信済みと草稿保存済みの状態が区別されないこと、未充足終了の再検討分岐で草稿をtask payloadにしか保存しないこと、保存例外を生成結果の検証エラーとして扱う余地があることである。

候補ではcursorsを配信済み範囲として維持し、pendingReadとcommittedCursorsを分離する。新規ジョブの読解開始位置はcommittedCursorsから求める。引用可能範囲は実配信済みを上限とし、現在配信中の本文からの引用を認める。草稿・行動の検証が成功して保存する際に、本文、Knowledge、根拠、カーソル、pendingReadの解除を同じDBトランザクションへ入れる。再検討分岐でも成果物を保存する。保存中の例外をLLM出力の訂正に変換しない。

committedは「本文を渡した応答を検証して草稿を保存した」という受領記録であり、その全内容が意味的に反映された保証ではない。不要な資料をすべて本文に書かせる変更ではない。同一草稿が有効なら範囲を受領できる。旧ジョブはcontentをpendingReadへ読み替えるが、過去の保存範囲を推測して新しいcommittedCursorsを生成しない。

期待効果は状態の整合と保存失敗への耐性であり、探索品質の上昇は未検証。検証失敗・再開で範囲が進む、成果物なしで保存済みになる、再試行でLLM呼び出しが重複する場合は機械的に不合格。品質は固定採点、引用、本文消失、正常完了、実測コストで判定する。

## 検証条件

専用テストは草稿訂正、訂正失敗、不正行動、DB保存失敗、再検討、旧状態の再開、長文の連続読解。各tickでEngineを生成し直す。DB保存失敗は書き込み実行後にLeaseLostでtransactionをrollbackし、呼び出し数・成果物・pending状態を確認する。

初期対はPostgreSQL 19、基準→候補。同日・近接時間、ガイドの通常予算。基準は `/tmp/deepstill-p0-baseline-20260914` の独立checkoutとDBで実行し、候補の編集から隔離する。基準実行ID：`e2e13d3f-eeca-4a70-b114-ab2f75121297`。証跡は `data/evaluations/luna-p0-20260914/`。URL・本文hash・失敗状況を各実行から保存する。

実装保存ブランチは `codex/experiment-luna-p0-20260914`。実装コミットを保存・pushした後に候補のlive実行を開始する。評価結果は後追記し、改善と確定するまでは候補扱いとする。

## 初期試行の結果と保存状態

実装コミット `88c6f48a6717dba5440cb694a528348a5f3f6754` は `codex/experiment-luna-p0-20260914` としてpush済み。全体検証は38ファイル251テストを含めて成功し、最終の表示値調整後も関連25テストが成功した。

|項目|基準|P0候補|
|---|---:|---:|
|固定評価（参考）|47|61|
|入力tokens|73,924|191,270|
|出力tokens|10,238|17,950|
|検索 / URL / 保存資料|1 / 4 / 2|2 / 12 / 7|
|終了|partial / invalid_deliverable|partial / unresolved_questions|
|時間|約237秒|約441秒|

今回の2試行累積は入力265,194・出力28,188。過去7試行を含む累積は入力1,725,560・出力201,792。不明量は別記し、ゼロ扱いしていない。

候補実行ID：`7cf652a0-51bb-40ab-92aa-10f83898db03`。通常WebUIで本文・終了状態・使用量を確認。基準DBは独立checkoutにあり、`data/evaluations/luna-p0-20260914/baseline/database.sqlite` にも保存した。候補は通常DBから参照できる。両試行の生ログ、操作監査、本文各版、引用照合、メモリ、URL・hash・失敗、最終task状態をdata配下へ保存した。

候補だけ追加検索がタイムアウトし、以後検索不可となったため、この対は採用の勝敗集計から除外する。点数と費用は参考値。候補では本文の技術説明が増えたが、token消費も約2.49倍。旧版の注記脱落とEpisodeの事実混同が残る。正常完了はどちらも0/1。

**採用保留**。mainの候補コードはrevertし、実装と追加テストは保存ブランチに保持する。廃案として削除したものではない。4テーマ16実行は未実施であり、優先度1以降を重ねていない。P0の品質改善を主張するには比較可能な新規対が必要。保存ブランチからの再開時は、mainのrevertとの関係を確認し、対象実装だけを再適用する。単純mergeでは復元されない場合がある。

# P0基準試行：47 / 100

候補の結果を見る前に採点。2026-09-14、PostgreSQL 19、e2e13d3f-eeca-4a70-b114-ab2f75121297。partial / invalid_deliverable。

|項目|点|根拠|
|---|---:|---|
|対象同定と回答|10/15|対象版・変更領域・ベータ情報と最終版の区別を説明。ただし製品全体の理解より検証報告の紹介に偏る。|
|探索・情報増分|8/25|公式から検証報告、本文リンクPDF、独立資料へ進んだが、1検索4取得で停止。拒否済みの公式URLそのものを再選択し、訂正でも回復できない。2資料目の情報が成果物へ入らない。費用対情報増分1/5を含む。|
|根拠・条件|14/20|6段落の引用は読了した概要ページが支持。未読PDFとベータ情報の限界を明記。一次仕様の裏付けと測定条件は未確認。|
|説明|6/20|本文816文字。機能名の一覧が中心で、構文・仕組み・例・比較・運用影響を説明できていない。|
|Knowledge|4/12|1件の領域横断チェック方針。対象条件はあるが、具体的な判断・確認手順が薄い。|
|Episode|5/8|1件、失敗終了とガードを記録。拒否済みURLの再選択を独立資料での補完失敗と説明するなど、行動の解釈が曖昧。|

event22で概要を読みversion1へ保存、event37でJPUG PDFを配信したが、delivery9/10の次行動検証で停止。最終version2はversion1と同じ816文字。訂正入力には同じPDF本文が再送されており、読み飛ばしは観測されない。未保存の生成結果を有効な成果物の消失とは呼ばないが、読解の成果を残せていない。

実測入力73,924・出力10,238、合計84,162。ガードで未読のcrawl2/6は使用量不明。実行は約237秒。WebUIの本文・使用量・partial表示を確認した。基準70点の過去試行は別テーマであり、この47点との単純比較はしない。


# P0候補試行：61 / 100（参考値、採用保留）

2026-09-14、PostgreSQL 19、7cf652a0-51bb-40ab-92aa-10f83898db03。partial / unresolved_questions。

|項目|点|根拠|
|---|---:|---|
|対象同定と回答|11/15|Beta 3の公式告知まで確認し、候補と最終仕様を区別。未公開かもしれない最終版の確定を中心課題にしてしまい、現時点の利用可能な機能を十分に確定できない。|
|探索・情報増分|14/25|7資料、本文リンク、公式文書・トップ・一覧へ進み、独立PDFの技術説明を成果物へ残せた。一方、同じ提供元の告知の重複と最終版探しが多い。追加検索タイムアウト。費用対情報増分2/5を含む。|
|根拠・条件|14/20|引用位置は配信本文内。候補機能と正式仕様を区別し、性能の再現条件不足を明示。古い開発資料とbeta時点の採用状況を十分に照合できず、途中稿のbeta2取り下げ注記も消えている。|
|説明|12/20|本文1634文字。部分リフレッシュ、REPACK、SQL/PGQ等の構文・動作と測定例が入る。目的別の比較・実運用条件は薄く、列挙中心の段落も残る。|
|Knowledge|6/12|版段階の確認とベンチマーク条件の2件。再利用の条件はあるが、技術の仕組みや具体的手順より一般的な注意則に寄る。|
|Episode|4/8|1件、検索タイムアウト・404・未解決を記録。ただし2月の開発資料の例をBeta 3時点の例のようにまとめ、ガードで全文未読の94ページPDFを一部未読のように説明する。|

## 機械的な確認と限界

read.committedは7件。最終committedCursorsとcursorsは7資料すべてで一致し、pendingReadは残っていない。カーソル更新と成果物保存は同じtransaction。検証失敗と保存失敗からの回復は専用テストで確認したが、このlive試行ではdeliverable.invalid自体が0件であり、その回復効果を実測したとは言えない。DB障害も発生していない。

最終版8は版7と同じ。版1にあったbeta2のバイナリpg_dumpall取り下げ注記は版2以降で消えた。保存済みカーソルは情報の意味的保持を保証しない。未解決のため正常完了ではない。入力191,270・出力17,950、合計209,220（基準の約2.49倍）、約441秒。

ガード未読のcrawl2/6/12/18/22と検索poll10は使用量不明。候補だけ追加検索がタイムアウトし、searchUnavailableにより以後の検索が止まった。この対は比較条件が揃わず、採用の勝敗集計から除外する。点数と費用は参考値として残す。両者が取得した概要ページ・JPUG PDFの本文hashは一致した。

61対47は単一の外部条件が異なる試行であり、P0の因果的な品質改善、元の70点基準を超えたこと、80点達成を示さない。4テーマ16実行の採用評価は未実施。コードは保存ブランチへ保持し、mainは採用済み基準へ戻す。廃案ではなく採用保留。
